import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingTop: 60,
    paddingBottom: 120,
    marginTop: 40
  },
  logo: {
    height: 250,
    maxWidth: '50%',
    resizeMode: 'contain'
  },
  buttons: {
    marginTop: 20
  },
  button: {
    marginBottom: 15
  }
});

export default styles;
