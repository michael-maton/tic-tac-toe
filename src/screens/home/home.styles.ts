import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 120,
    marginTop: 40
  },
  logo: {
    height: 250,
    maxWidth: '100%',
    resizeMode: 'contain'
  },
  buttons: {
    marginTop: 20
  },
  button: {
    marginBottom: 10
  }
});

export default styles;
